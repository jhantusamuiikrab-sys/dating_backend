1. Before uploading image name will change with one unique name and same image can upload again and again by changing the name.
2. get api for gallery section. 
3. how to delete particular image or a complete gallery. 



seperate the image conveter section in the helper section
seperate the multer upload secion in the middleware function


(01-02)/12/2025
1.if 2 many file uploaded then send proper response
2.if i'm uploading 5 image at the same time then 5 image got the same name but image name should be different #done
3.update single images on the album #done
4.update album name #done
5.delete album #done
7.delete any single images on that album #done
8. every place I did batchTitle I have to change it albumTitle and albumName #done
9. change the name in the api also #done
10. have to create a seperate route for album #done
11. in postman also have to create a seperate folder have to seperate the route as it easily accassable #done
12. transper code from helper function to utils function #done
13. have to upload code on github #done
14. have to delete unnecessary packages like crypto that has not make any sense #done
15. to remove the upload image controller we have to do the necessary changes #done



03/12/2025
1. token based login
2. username , mobile no , gmail based login
3. for username password based login
4. for mobile no and gmail otp based login


05/12/2025
1. Send OTP for email update and verify
2. if mobile no not change then rest of the process will stop #done


08/12/2025
1. Reset password #some problem occured during rest password

09/12/2025
user register with ph no ->  then if we want to update user password then login not need and for login also password not need initally
page open - ph no- verify- name , pass, email, state city - login 
if email/ph verify then reset password
after resgistration process completed then we generate pin


11/12/2025
1. At the time of update the user encrypt the password
2. In the getPassword route we decrypt the password and return the original password in the response
3. In the login controller we have to decrypt the password
4. | Purpose              | Field to Check                       | Why                  |
| -------------------- | ------------------------------------ | -------------------- |
| Validate account     | `email` OR `phoneno`                 | To find the user     |
| Verified identity    | `isMobVerified` OR `isEmailVerified` | For secure PIN login |
| Ensure no old PIN    | `hasloginpin`, `loginpin`            | Avoid duplicates     |
| Ensure active status | `isDeleted`, `isActive`              | Only for valid users |

12/12/2025
1. Reset PIN should be manually not auto generated
2. Generate and reset should be combined in one function if pin is not present then generate for 1st time but if already present the reset new one
3.** For PIN based login created one field deviceId a random id and that is stored in the database
4. 
